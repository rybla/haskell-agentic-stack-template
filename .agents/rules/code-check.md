---
trigger: always_on
glob:
description: Run all source code checks with `just check`
---

After editing any source code, you must run `just check`, which will output diagnostics collected from running all source code checks. You must iteratively address these diagnostics and re-run `just check` until all diagnostics have been addressed.

